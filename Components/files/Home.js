import React from "react"
import { Text } from "react-native"

const Home = props => (
    // <Burger navigation={props.navigation}>
        <Text>Page Home !!</Text>
    /* </Burger> */
)

export default Home