import React from "react";
import { Text, View } from "react-native";
import Constants from 'expo-constants';

const AddEvent = () => (
    <View>
        <Text style={{marginTop: Constants.statusBarHeight}}>Add Event Screen</Text>
    </View>
)

export default AddEvent;